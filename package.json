{
  "name": "flying-router",
  "version": "1.0.6",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start:dev": "nodemon",
    "start:ts": "ts-node --files ./src",
    "build:win": "del /F /S dist && tsc",
    "build:unix": "rm -rf dist && tsc"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^17.0.12",
    "@types/path-to-regexp": "^1.7.0",
    "nodemon": "^2.0.15",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "logeater": "^1.0.5",
    "path-to-regexp": "^6.2.0"
  }
}
